{shared_opening_block}

{shared_runtime_context_block}

This call is TypeFly REPLAN / CONTINUATION planning, not a fresh plan from zero.
Use continuation context, preserve completed progress, and re-optimize remaining route safely.

Continuation context:
- previous plan: {previous_plan}
- execution history: {execution_history}
- mission progress: {mission_progress}
- completed checkpoints: {completed_checkpoints}
- remaining checkpoints: {remaining_checkpoints}
- current target checkpoint: {current_target_checkpoint}
- replan runtime history:
{replan_history_block}

TypeFly replan requirements:
- Explicit replan threshold = 0.5.
- If current_collision_probability < 0.5 and geometry is clear, prioritize checkpoint progress and efficiency.
- Even below threshold, still evaluate close-pass risk via worker geometry, UAV heading, and radii.
- If current_collision_probability >= 0.5, or geometry is clearly unsafe:
  - first output one or more conservative low-level avoidance actions,
  - do not begin with direct gc(...),
  - choose avoidance direction with UAV heading awareness,
  - match avoidance amplitude to severity.
- After avoidance, continue remaining checkpoints only; never redo completed checkpoints.
- Remaining checkpoint order may be re-sorted dynamically by safety + geometry + efficiency.
- The same ml/mr/mf/mb maps to different world directions under different headings; keep this heading-aware in replan.

Guides:
{guides}

TypeFly replan examples:
{typefly_replan_examples}

Task description:
{task_description}

response:
